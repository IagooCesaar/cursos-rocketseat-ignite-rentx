{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "docker",
  "password": "ignite",
  "database": "rentx",

  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "migrationsTableName": "_migrations",
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}